{
  "name": "airflow-registry",
  "version": "1.0.0",
  "description": "Apache Airflow Provider Registry",
  "scripts": {
    "dev": "pnpm build && eleventy --serve --port=8080",
    "build": "eleventy",
    "postbuild": "node scripts/build-pagefind-index.mjs"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.1.0"
  }
}
